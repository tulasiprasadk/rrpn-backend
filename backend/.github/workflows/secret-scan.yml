name: Secret Scan

on:
  push:
  pull_request:

jobs:
  secret-scan:
    name: Secret pattern scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run secret pattern checks
        run: |
          chmod +x ./scripts/check-secrets.sh || true
          ./scripts/check-secrets.sh
