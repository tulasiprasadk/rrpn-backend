# =====================================
# RR Nagar Backend Environment Config
# =====================================

# Server
NODE_ENV=development
PORT=5000

# Database (SQLite)
DB_STORAGE=./database.sqlite

# Session Secret
SESSION_SECRET=your-secret-key-change-this-in-production

# SMS Configuration
# Options: 'console' (development), 'twilio', 'msg91', 'fast2sms'
SMS_PROVIDER=console

# Twilio Configuration (if using SMS_PROVIDER=twilio)
# TWILIO_ACCOUNT_SID=your_account_sid
# TWILIO_AUTH_TOKEN=your_auth_token
# TWILIO_PHONE_NUMBER=+1234567890

# MSG91 Configuration (if using SMS_PROVIDER=msg91)
# SMS_API_KEY=your_msg91_api_key
# SMS_SENDER_ID=RRNAGR
# SMS_TEMPLATE_ID=your_template_id

# Fast2SMS Configuration (if using SMS_PROVIDER=fast2sms)
# SMS_API_KEY=your_fast2sms_api_key
# SMS_SENDER_ID=RRNAGR

# Email SMTP (for email OTP/login)
# Generate app-specific password from your email provider (do NOT use login password)
SMTP_HOST=smtp.zoho.in
SMTP_PORT=465
SMTP_USER=namaste@rrnagar.com
SMTP_PASS=your-app-password-here
SMTP_FROM=namaste@rrnagar.com

# CORS allowed origins (comma-separated)
# Include all frontend origins that will call this API
# Example for production:
# CORS_ORIGINS=https://www.rrnagar.com, https://rrnagar.com, https://your-project.vercel.app, https://tulasiprasadk.github.io

# Admin Settings
ADMIN_EMAIL=admin@rrnagar.com
ADMIN_PASSWORD=admin123

# WhatsApp Configuration (optional)
# WHATSAPP_API_URL=your_whatsapp_api_url
# WHATSAPP_TOKEN=your_token
