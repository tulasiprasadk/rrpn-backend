# Example production environment variables
PORT=4000
DATABASE_URL=postgres://user:password@db:5432/subscriptions
ALLOWED_ORIGINS=https://www.yourshop.com
NODE_ENV=production
# GHCR credentials are provided to GitHub Actions via secrets
